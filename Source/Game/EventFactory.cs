namespace Game.Game;

public class EventFactory
{
    // public static GameEvent PlayerFireEvent(object sender) => new(sender, ("player_action", "fire"));
    // public static GameEvent PlayerMoveEvent(object sender) => new(sender, ("player_move", "forwards"));
}
